package stepDefinations;

import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;
import io.cucumber.java.Before;

public class Hooks {

	/*public static String place_id;
	public static String partialCode;
	StepDefination sd = new StepDefination();
	
	@Before("@DeletePlace")
	public String beforeScenario(String place_id) throws IOException {
		// write a code that will gave you place id
		// execute this code only if place id is null

		//StepDefination m = new StepDefination();
		
		if(StepDefination.place_id == null) {

			String text; 
	        FileReader f = new FileReader("C:\\Users\\carlos.banuelos\\eclipse-workspace\\APIFramework\\pid"); 
	        BufferedReader b = new BufferedReader(f); 
	        while((text = b.readLine())!=null) { 
	        	partialCode = text.split("place_id=")[1];
	        } 
	        
	        b.close(); 
	       
			//m.add_place_payload_with("Carlos", "Spanisg", "Mexico");
			//m.user_calls_with_http_request("AddPlaceAPI", "POST");
			//m.verify_place_id_created_maps_to_using("Carlos", "POST");
		}
		 place_id = partialCode;
		 System.out.println(place_id);
	     return place_id;
	}
*/
}
